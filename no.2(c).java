      if (bookTitle.equalsIgnoreCase(userInput)) {
       // Match found, proceed with the search result
   }
   

 toLowerCase(): This method converts a string to lowercase. By converting both the book title and the user input to lowercase before comparison, the system ensures that searches are case-insensitive.

      if (bookTitle.toLowerCase().equals(userInput.toLowerCase())) {
       // Match found, proceed with the search result
   }
